<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.drivesafe1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditImage"></activity>
        <activity android:name=".EditEmail" />
        <activity android:name=".EditPassword" />
        <activity android:name=".TypeSelect">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".OwnerSignUp" />
        <activity android:name=".UserLogin" />
        <activity android:name=".OwnerLogin" />
        <activity android:name=".SettingsActivity" />
        <activity
            android:name=".OwnerMainActivityMain"
            android:label="@string/title_activity_owner_main_main"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>